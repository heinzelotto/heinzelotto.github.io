# Moved

The ArgMap authoring tutorial now lives at the site root:

<https://www.p1graph.org/AUTHORING_TUTORIAL.md>

The app moved from `p1graph.org/argmap/` to `p1graph.org/` on 2026-07-29
(DECISIONS.md D62). Fetch the URL above instead of this one.
